# 내차위치 — 삼성 실기기 테스트 가이드

## 최초 1회: 릴리스 APK로 재설치

자동 업데이트는 릴리스 서명 APK 기준이라, 디버그 APK가 깔려 있으면 **한 번 삭제 후**
https://github.com/dacisosl/CARwhere/releases 의 최신 `naechawichi-v*.apk`로 설치한다.
이후부터는 앱을 열면 대시보드 상단에 "새 버전" 배너가 뜨고, 탭 → 다운로드 → 설치 확인 1탭으로 갱신.

## v5.5 추가 확인 항목 (하차 → 시트 즉시)

- [ ] 차 시동을 끄고(블루투스 끊김) **기다림 없이** 기록 시트가 뜨는지 (체감 1초 이내)
- [ ] 시트가 먼저 뜨고, 등록된 주차장이면 1~2초 안에 그 위치 이름·층 구성으로 바뀌는지
- [ ] 화면이 잠긴 상태에서 시동을 끄면 잠금화면 위에 시트가 보이는지 (화면이 저절로 켜지지는 않아야 정상)
- [ ] **오탐 되돌리기**: 시동을 끄고 시트가 뜬 뒤 8초 안에 다시 시동을 걸면 —
      시트가 닫히고, 상태바 캡슐도 사라지고, 주차 기록(전체 보기)에 그 건이 남지 않는지
- [ ] 8초가 지난 뒤 시동을 걸면 정상 출차로 처리되는지 (캡슐 사라짐 + 기록에는 남음)
- [ ] 층을 고른 뒤 곧바로 시동을 걸면 그 기록이 지워지지 않고 히스토리에 남는지
- [ ] 홈 맨 아래 "테스트: 하차 시뮬"로도 같은 흐름이 재현되는지

## v5.4 추가 확인 항목 (잠금화면 Now Bar · 확인 카드 삭제) — Android 16 · One UI 8

- [ ] 주차 기록 후 **화면을 잠그고** 상단 상태바에 [층 아이콘 + "B1"] 칩이 보이는지,
      하단 **Now Bar**에도 뜨는지, 탭하면 층·경과시간이 확장되는지
- [ ] 층을 바꾸면(◁ ▷ 다시 기록) 칩 글자가 바로 바뀌는지, 출차하면 칩이 사라지는지
- [ ] 칩이 안 뜨면 순서대로: ① 홈에 "잠금화면 Now Bar 표시가 꺼져 있어요" 배너가 보이는지 → [켜기]
      ② 설정 > 알림 > 어따뒀지 > **실시간 업데이트(Live updates)** 토글 켜기
      ③ 폰 설정 > 잠금화면 및 AOD > **Now bar** > 실시간 업데이트 허용
      ④ 앱 설정 > 알림 > "잠금화면 Now Bar" 행이 "사용 중"으로 바뀌는지
- [ ] 업데이트 설치 직후 주차 캡슐이 새 채널(주차 위치 표시)로 다시 올라오고 **소리·진동이 없는지**
- [ ] Android 15 이하 기기: 잠금화면 알림에 층·경과시간 내용이 가려지지 않고 보이는지
- [ ] 기록 카드에서 [주차] → 확인 카드("맞아요?") 없이 토스트로 바로 닫히는지.
      설정 > 바텀시트에 "등록 확인" 행이 없는지

## v5.3.1 추가 확인 항목 (기록 카드 격자 · 차 사진 전체)

- [ ] 홈 차 사진이 잘리지 않고 **사진 전체**가 보이는지 (천장 조명·주차 구획선·차 전체).
      좌우에 어두운 여백이 생기는 것은 정상 — 사진을 자르지 않기 때문
- [ ] 기록 카드: ◁ · 층 박스 · ▷ 가 같은 높이(56dp) 한 선에 있고, 그 아래
      위층/지난번/아래층 캡션이 같은 선에 있는지
- [ ] [주차] 버튼이 위 두 줄을 합친 높이로 오른쪽을 채우는지
- [ ] 헤더의 위치 칩과 [📷 사진] 버튼 높이가 같은지 (34dp)
- [ ] ◁ 위층은 파랑, ▷ 아래층은 빨강으로 화살표·캡션이 같은 색인지.
      맨 위층/맨 아래층에서 해당 쪽이 회색으로 비활성되는지

## v5.3 확인 항목 (Pretendard·층수·차 사진·위치 칩)

- [ ] 앱 전체 글씨가 Pretendard로 바뀌었는지 — 특히 한글 제목·본문, 위젯 숫자,
      상태바 층 표지판, 스플래시 "어따뒀지" (삼성 기본 서체 인상과 달라야 정상)
- [ ] 홈 층수: 카드·색판 없이 **딥 파인 그린 숫자만** 크게 (미주차는 회색 "—")
- [ ] 홈 오른쪽 타일: 실제 주차 사진이고 "주차" 글자가 없는지, 설정에서 차종·색을
      바꾸면 사진도 바뀌는지 (경차/중형/SUV/스포츠 × 흰/검/회색 12장)
- [ ] 사진을 찍으면 그 사진이 타일을 채우고, 캡션이 "사진 보기"로 바뀌는지
- [ ] 기록 카드 층 박스는 **테두리 판**(흰 바탕 + 그린 테두리·글자) — 옆의 그린 [주차] 버튼과 구분되는지
- [ ] 상태바·알림 표지판은 여전히 층별 색(B1 라임 … 1F 앰버)인지 (앱 화면만 그린으로 바뀐 것)
- [ ] 위치를 4개 이상 등록하고 홈 지도 칩 줄에서:
      오른쪽 끝에 페이드가 보이는지 / 옆으로 스크롤되는지 /
      위치관리에서 4번째 이후 위치를 고르면 그 칩으로 자동 스크롤되는지

## v5.2 확인 항목 (가독성·구분·반응형)

- [ ] 카드와 바탕이 확실히 구분되는지 (카드 흰색 + 옅은 테두리·그림자, 바탕 회끼)
- [ ] 홈이 **스크롤 없이** 한 화면에 들어오고, 지도가 하단 탭 바로 위까지 채워지는지
  - [ ] 기종/화면 크기가 다른 폰 2대에서 지도 밑 여백이 없는지
- [ ] 헤더 로고: [P] 그린 배지 + "어따뒀지" 굵은 잉크 (네온 글로우 없음)
- [ ] 층 타일이 사각 표지판이고 층 색이 상태바 아이콘과 같은지 (B1 라임 / 1F 앰버 …)
- [ ] 오른쪽 타일: 어두운 주차 구획에 내 차 + 그린 언더글로우, 사진 찍으면 사진이 채움
- [ ] 사진 캡션에 카메라 아이콘이 있는지
- [ ] 지도가 일반 지도 색(반전 없음), 내 위치 파란 점 / 차 마커 검정 + 흰 링
- [ ] 라이트 모드가 기본, 설정 > 테마 첫 항목이 "라이트 (기본)"
- [ ] 설정 항목의 라벨·값이 겹치지 않는지 (특히 "바텀시트 팝업 (다른 앱 위)" 행), ★ 별표 사라졌는지
- [ ] 기록 시트: [주차]가 오른쪽 큰 버튼, 사진은 헤더 작은 버튼(찍으면 썸네일 + "사진 1")
- [ ] 기록 시트 하단 안내 문구가 없어졌는지
- [ ] **10초 지나도 시트가 안 내려가는지**, 그 사이 상태바에 P 캡슐이 이미 떠 있는지
- [ ] 홈 맨 아래 "테스트: 하차 시뮬" 탭 → 5초 뒤 기록 시트 (릴리스 빌드에서도 보임)
- [ ] 위젯 글씨가 이전보다 두꺼운지 (sans-serif-black), 2x1 한 줄에 [B1] [3:14]
- [ ] 앱 아이콘·스플래시 배경의 라임 액센트가 그린으로 바뀌었는지

## v5.1 추가 확인 항목 (기록 카드 · 위젯 H:MM)

- [ ] 하차 감지 → 기록 카드: 위치 칩(집 / ⊕ 위치 등록) + [주차], ◁ B1 ▷, 사진 슬롯, 메모 한 줄
- [ ] ◁ ▷로 층 이동 (지하 그린 / 지상 버건디), 기압 추정·지난번 층이 초기값으로 잡히는지
- [ ] 사진 슬롯 탭 → 카메라 → 썸네일이 슬롯에 채워지는지
- [ ] 메모 입력 후 [주차] → 확인 카드(설정 시) → 맞아요 → 상태바·위젯에 층 표시
- [ ] "⊕ 위치 등록" → 이름·층 구성 저장 → 카드로 돌아오고 칩에 이름이 뜨는지
- [ ] 10초 무응답 시 시트가 내려가고 "위치만 저장됨"인지
- [ ] 위젯 2x1: 한 줄에 [B3] [3:14], 위아래 잘림 없음. 가로로 넓히면 주차 시각·장소 추가
- [ ] 위젯 1x1: 층수 + H:MM
- [ ] 위젯 시간이 1분 안에 갱신되는지 (화면 켜진 상태), 출차 후 갱신 멈춤
- [ ] 재부팅 후 위젯 시간이 다시 흐르는지

## v5.0 추가 확인 항목 (하단 탭 · 새 팔레트)

- [ ] 앱이 **흰 바탕**으로 뜨는지 (기본 테마 라이트), 상태바 아이콘이 어둡게 보이는지
- [ ] 하단 탭 홈 / 위치관리 / 설정 전환, 탭 상태가 회전 후에도 유지되는지
- [ ] 홈: 경과시간 바(숫자 버건디) → 타일 2개(층수 엔진 버튼 그린 링 / 사진 또는 내 차) → 지도 카드 순서
- [ ] 사진 타일: 주차 중 사진 없으면 탭 → 카메라, 찍은 뒤 타일에 사진이 채워지는지, 탭 → 크게 보기·다시 찍기
- [ ] 지도 칩: 집 탭 → 지도가 집으로 이동 + P 핀, 주차 중이면 차 마커와 함께 프레이밍
- [ ] 검색 칩: "집" 입력 → 집 선택 / "스타필드 하남" 입력 → 지오코딩 핀 / 엉뚱한 말 → "찾지 못했어요"
- [ ] 위치관리 탭: 목록 · "+ 추가" · 탭 → 편집 모달 · 주차 중 위치에 "지금 여기" 배지
- [ ] 설정 탭: 뒤로 버튼 없이 바로 설정, "주차 기록 → 전체 보기" 동작
- [ ] 층 선택(휠·버튼): 지하 = 그린, 지상 = 버건디로 구분되는지
- [ ] 홈 위젯: 흰 카드, 층수 그린, 경과 시간 버건디
- [ ] 스플래시 워드마크가 그린 계열로 점등되는지 (영상 속 라임 링은 영상 자체 — 재제작 필요 시 별도)
- [ ] 다크 테마로 바꾸면 잉크 바탕 + 밝힌 그린/버건디로 일관되게 보이는지

## v4.3 추가 확인 항목 (위젯 타이머·타이포·상태바 표지판)

- [ ] 홈 위젯 2x1: 경과 시간이 **말줄임 없이** 전부 보이는지 (40:52:39처럼 8자여도)
- [ ] 홈 위젯 1x1: 층수 + 경과 시간, 잘림 없음
- [ ] 위젯 시간이 1초씩 흐르는지 (앱을 안 열어도 — 런처가 그린다)
- [ ] 100시간 넘게 주차하면 "4일 21시간"으로 바뀌는지
- [ ] 상태바: 층 표지판이 이전보다 크고, 글자가 표지판을 꽉 채우는지
- [ ] 상태바 색: B1(라임) / B2(연두) / B3(초록) / 1F(앰버) / 2F(오렌지)가 서로 다른지
  - 색이 보존되지 않는 기기면 흰 표지판 + 글자로 보이는 게 정상 (층 구분은 글자로)
- [ ] 알림창 배경색도 층 색으로 바뀌는지
- [ ] 스플래시: "어따뒀지"가 글자마다 형광등처럼 점등 → 하이라이트 스윕 → 태그라인
- [ ] 대시보드 헤더 워드마크에 네온 글로우 + 밑선이 보이는지
- [ ] 대시보드: 엔진 버튼이 왼쪽으로 붙고 커졌는지, 오른쪽 원에 앱 아이콘이 박혔는지
- [ ] 대시보드: 차량 신호 진단 줄이 한 줄로만 나오는지
- [ ] 대시보드: 경과 시간 칩에 값만 크게 (주차 시각 캡션 없음)
- [ ] 위치·설정·최근 주차 라벨이 잘 보이는지 (색·크기 상향)
- [ ] 위치 편집: 제목 오른쪽에 저장 + 휴지통, 휴지통 → 확인 모달 → 삭제
- [ ] 위치 편집: 본문 스크롤바가 형광으로 보이는지

## v4.2 추가 확인 항목 (블루투스 감지 수정 + 대시보드 3카드)

- [ ] **감지 진단**: 시동 걸기(BT 연결) → 대시보드 헤더 아래 "마지막 차량 신호 · 시각 연결"이 갱신되는지
  - 이 줄이 안 바뀌면 리시버가 깨어나지 않는 것 → 근처 기기 권한·배터리 예외·내 차 지정 확인
- [ ] 시동 끄고 하차 → "마지막 차량 신호 · 끊김" 갱신 + 5초 뒤 바텀시트/알림
- [ ] 이어폰·워치 연결/해제 → "마지막 차량 신호"가 바뀌지 **않아야** 함 (내 차 MAC 필터)
- [ ] 주차 카드: 엔진 버튼(층수) 탭 → 다시 기록 / 내 차 아이콘(설정에서 고른 차종·색) 표시 / [사진] [+HH:MM 경과]
- [ ] 지도 카드: 위치 칩(집·학교…) 탭 → 수정 모달, "+" → 추가 / 지도에 차 마커 + 내 위치 점선 + "차까지 320m · 북동쪽"
- [ ] 지도 탭 → 지도 앱(구글/네이버/카카오)으로 차 좌표 열림
- [ ] 지하에서 GPS 못 잡은 경우 "저장된 차 좌표가 없어요" 안내
- [ ] 설정 카드: ★ 즐겨찾기 행 항상 표시, "더 보기" → 아코디언 펼침(나머지 빠른 설정·내 차·배터리 예외), "모든 설정 →"
- [ ] 라이트 테마에서 지도 타일이 반전되지 않고 일반 색으로 보이는지
- [ ] 홈 위젯 2x1: 층수(네온) 옆에 **경과 시간이 크게** 흐르는지 (1초 틱), 아래 "주차 13:43 · 장소"
- [ ] 홈 위젯 1x1: 층수 + 작은 경과 시간. 1시간 넘으면 "1:02:15"로 자리수가 늘어도 잘리지 않는지
- [ ] 위젯 경과 시간이 흐르는 동안 앱 프로세스가 깨어나지 않는지 (개발자 옵션 > 실행 중인 서비스)

## v3.2 추가 확인 항목

- [ ] 설정 > 앱 아이콘 > 차량 아이콘: 차종(경차/중형차/SUV/스포츠카) × 색상(흰/검/회) 선택 → 적용
- [ ] 홈 화면 아이콘이 선택한 차량("주차여기" 사인 디자인)으로 바뀌는지 (잠깐 재시작될 수 있음)
- [ ] 바꾼 뒤에도 알림 탭·위젯 탭으로 앱 진입이 되는지
- [ ] "기본 아이콘(형광)으로 되돌리기" 동작
- [ ] 대시보드 업데이트 배너: 새 릴리스가 있으면 표시 → 탭 → 다운로드 → 설치 화면

## v3 추가 확인 항목

- [ ] 앱 이름·아이콘: "내차위치" + 주차칸에 세워진 형광 차 아이콘
- [ ] **새로운 곳 주차** → 바텀시트가 "위치 설정(이름+층 구성)"을 먼저 요구 → 저장 후 층 선택
- [ ] **등록된 위치 주차** → 그 위치의 층 구성이 바로 나옴 (150m 매칭)
- [ ] 층 선택 후 **자동으로 세부구역 입력창** (기본값 층+메모) — 설정에서 층수만/층+사진으로 변경 가능
- [ ] 위치별 바텀시트 모드: 설정 > 위치 > (탭, 모달창) > 바텀시트에서 개별 지정
- [ ] 층 구성: 지상 1~3층 + 지하 1~3층 기본, "+"로 4F/B4… 확장, 탭으로 선택
- [ ] 설정 순서: 위치 / 바텀시트 / 알림(상태바만·홈위젯만·둘다) / 감지(자동감지 기본 꺼짐) / 기타
- [ ] 알림 "홈 위젯만" 선택 시 상시 알림이 안 뜨고 위젯만 갱신되는지
- [ ] 위젯: 층수+주차시각 표시, 필 위젯 차 아이콘 커짐

## v2 추가 확인 항목

- [ ] 온보딩 2단계에 "다른 앱 위에 표시" 권한 추가됨 — 허용 시 하차 감지 때 **바텀시트**가 화면 하단에 뜸
- [ ] 오버레이 권한 거부 시 → 기존 헤드업 알림으로 폴백
- [ ] 바텀시트: 층 탭 형광 점등 → 위로 쓸면 구역/메모 입력, 10초 무응답 시 하강
- [ ] 대시보드: 원형 게이지 + 최근 주차 카드 + 설정 카드(탭하면 펼침), 상태바/내비바 영역 침범 없음
- [ ] 홈 위젯: 길게 눌러 위젯 추가 → "주차어디" 2종 (형광 필 / 미니 게이지), 주차 상태 바뀌면 갱신
- [ ] 기압 자동감지(베타): 설정에서 켜고 → 지하 2층 이상 내려가 주차 → 시트에 "기압 추정" 층 미리 강조되는지
- [ ] 주차 기록: 출차 후 대시보드 최근 주차 카드 + 전체 보기 화면에 기록 쌓이는지
- [ ] v1 위에 덮어 설치해도 설정·프로필 유지되는지 (applicationId 동일)

에뮬레이터로는 블루투스 ACL 이벤트를 재현할 수 없으므로, 감지 플로우는 반드시 실기기에서 확인한다 (CLAUDE.md 우선순위 3).

## 0. 설치

```
adb install app\build\outputs\apk\debug\app-debug.apk
```

또는 `app\build\outputs\apk\debug\app-debug.apk`를 폰에 복사해 직접 설치 (출처를 알 수 없는 앱 허용 필요).

## 1. 온보딩 (30초 목표)

- [ ] 1단계: 근처 기기 권한 허용 → 페어링 목록에 차량 표시 → 선택 시 형광 테두리+체크
- [ ] 1단계: "차량 블루투스가 없어요" 탈출구 동작 (수동 모드)
- [ ] 2단계: 알림 / 근처 기기 / 배터리 최적화 제외 — 전부 켜야 "다음" 활성화
- [ ] 2단계: **삼성 안내 카드** 표시 확인 → 설정 > 배터리 > 백그라운드 사용 제한 > 절전 예외 앱에 "어따뒀지" 추가
- [ ] 3단계: 표시 방식 선택 → "네, 층 기록하기" → 층 팝업 즉시 실행

## 2. 감지 플로우 (핵심 — 실차 테스트)

1. 차 시동 → 폰이 차량 BT에 연결됨
2. 주차 후 시동 끄고 하차 → **5초 후** 헤드업 알림 "주차하셨나요?" 확인
   - [ ] 층 버튼 (지난번 층이 첫 버튼) + "전체 보기" 표시
   - [ ] 층 버튼 탭 → 상태바에 "B3" 텍스트 아이콘 상시 표시
   - [ ] 10초 무응답 → "주차 위치 저장됨" (층 없음) 상태로 전환
3. 재탑승 (BT 재연결) → 상태바 아이콘 자동 제거
4. 오작동 필터:
   - [ ] 차 근처에서 BT가 순간 끊겼다 5초 내 재연결 → 팝업 안 뜸
   - [ ] **이어폰/워치** 연결 해제 → 아무 반응 없어야 함 (내 차 MAC 필터)

## 3. 절전 정책 생존 테스트 (최대 리스크)

- [ ] 절전 예외 등록 후, 앱을 최근 앱에서 스와이프 제거 → 1시간 뒤 하차 감지 여전히 동작?
- [ ] 재부팅 → 감지 서비스 자동 재기동 (상시 알림 "감지 대기 중" 존재) 확인
- [ ] 안 되면: 설정 > 배터리 > 절전 예외 앱 재확인, dontkillmyapp.com/samsung 참고

## 4. 상태바 아이콘 확인 항목

- [ ] "P·B3" 텍스트가 상태바에서 읽히는지 (일부 기기에서 톤다운/축소될 수 있음)
- [ ] One UI 잠금화면에서도 알림 표시되는지

## 5. UI 확인 (에뮬레이터로도 가능)

- [ ] 대시보드: 미니 단면 + 형광 층수 + 경과 시간, 주차 없을 때 회색 상태
- [ ] 디버그 빌드 하단 "[디버그] 하차 시뮬" 텍스트로 감지 플로우 UI 재현 가능
- [ ] 풀 팝업: 층 세로 스택, 지난번 층 강조, 슬라이드 업 200ms
- [ ] 설정: 4섹션, 바텀시트, 프로필 추가/편집/삭제
- [ ] 프로필: "현재 위치로 등록" 후 150m 내에서 수동 기록 → 커스텀 층 구성 표시

## 6. adb로 감지 시뮬레이션 (실차 없이)

디버그 빌드 대시보드의 "[디버그] 하차 시뮬 / 재탑승 시뮬" 텍스트 버튼 사용이 가장 간단.
(BT 브로드캐스트는 시스템 보호 브로드캐스트라 adb로 위조 불가)
